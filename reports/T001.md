# T001 — Monorepo scaffold

**Branch:** `feat/T001-monorepo-scaffold` (base: upstream `main` @ 46e7723)
**Delegation:** none — [architect] task per PLAN.md. Pure file layout with no
code generation, so delegation to a LOW-thinking worker adds risk without value.

## What was done

- `backend/` created with placeholder `app/__init__.py` (real FastAPI skeleton
  arrives in T003 — intentionally not pre-built).
- `frontend/` created with `.gitkeep` (real Vite skeleton arrives in T007).
- Per-package ignore files: `backend/.gitignore` (`__pycache__/`, `*.pyc`,
  `.pytest_cache/`, `.venv/`), `frontend/.gitignore` (`node_modules/`, `dist/`).
- `readme.md` renamed to `README.md` and rewritten with the repo layout section
  (backend, frontend, harness, scripts, tasks, reports, logs) + quick start.

## Verification

- `git status` confirms `.env` untracked before commit.
- No tests to run: repo contains no runnable application code yet (first
  runnable code lands in T003); `pytest`/`npm build` have nothing to execute.

## Token usage

Zero worker tokens (no delegation).
